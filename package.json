{
  "name": "ateney",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "esbuild src/main.ts --bundle --serve=8080 --outfile=dist/main.js",
    "build": "esbuild src/main.ts --bundle --minify --outfile=dist/main.js"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "typescript": "^5.5.0"
  }
}
